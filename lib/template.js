const { VERSION } = require('./index');

module.exports = `{
  "tips": [
    "Drag and drop JSON to start querying",
    "JSON5 is also supported",
    "Select from the examples under the 'File'"
  ],
  "version": "${VERSION}"
}`;
